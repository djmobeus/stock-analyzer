[project]
name = "uk-stock-analyzer"
version = "0.1.0"
description = "UK FTSE stock screening and analysis system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
